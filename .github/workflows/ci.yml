name: CI

on:
  push:
    branches:
      - develop
      - feature/**

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # - run: echo "Running CI"
      - run: exit 1
