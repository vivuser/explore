name: CI

on:
  push:
    branches:
      - develop
      - feature/**
  pull_request:
    branches:
      - develop

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Simulate CI Failure
        run: |
          echo "Running CI pipeline..."
          echo "Simulating failure to test branch protection"
          exit 1      

          
     